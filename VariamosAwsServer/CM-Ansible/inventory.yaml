webservers:
  hosts:
    variamosserver01:
      ansible_host: 3.130.187.131
      # SSH key file
      ansible_ssh_private_key_file: ./variamos_prd.pem
      # SSH user
      ansible_ssh_user: ubuntu

datacenter:
  children:
    webservers:

